{
  "name": "zeronautt-solana-agent-allowance-demo",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "A TypeScript demo for bounded AI-agent API spend using Solana native subscriptions and recurring allowances.",
  "scripts": {
    "build": "tsc",
    "demo": "node dist/demo.js agent-api-allowance.json",
    "check": "npm run build && npm run demo"
  },
  "keywords": [
    "solana",
    "subscriptions",
    "allowances",
    "ai-agents",
    "stablecoin"
  ],
  "license": "MIT",
  "dependencies": {
    "@solana/kit": "^6.4.0",
    "@solana/subscriptions": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
